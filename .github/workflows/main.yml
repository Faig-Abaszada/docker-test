name: vue-app

on:
  push:
    branches: [ "main" ]

jobs:
  build-project:

    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
      - uses: actions/checkout@v2
      - name: Install NPM dependencies
        run: npm install
      - name: Build and Export
        run: npm run build

  docker-build-push:
    needs: build-project
    name: Build docker
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Login to Docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/portfolio



#
#
#
#
#      - name: Build
#        run: npm run build
#
#      - name: Push new version
#        run: git push
#
#      - name: Login to DockerHub Registry
#        run: echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
#
#      - name: Build Docker image
#        run: docker build . --file Dockerfile --tag laptoplufaik/portfolio
#      #replace victoriamartinsahagun with your DockerHub username
#      #replace vueapp with the image name
#
#      - name: Push to Docker Hub
#        run: docker push laptoplufaik/portfolio
##replace victoriamartinsahagun with your DockerHub username
##replace vueapp with the image name
